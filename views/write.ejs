<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Simple Blog using NodeJs</title>
  <style>
    /* Add your CSS styles here */
    body {
      font-family: Arial, sans-serif;
      background-color: #f5f5f5;
      margin: 0;
      padding: 0;
    }

    .header {
      background-color: #0f0a35;
      color: #fff;
      padding: 20px;
      text-align: center;
    }

    .container {
      background-color: #fff;
      border: 1px solid #ccc;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(48, 25, 25, 0.1);
      padding: 20px;
      margin: 20px auto;
      max-width: 600px;
    }

    .title {
      font-size: 24px;
      margin: 10px 0;
    }

    .author {
      font-size: 14px;
      color: #777;
      margin-bottom: 20px;
    }

    input[type="text"],
    textarea,
    input[type="file"] {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 3px;
      font-size: 16px;
    }

    input[type="submit"] {
      background-color: #333;
      color: #fff;
      border: none;
      border-radius: 3px;
      padding: 10px 20px;
      font-size: 18px;
      cursor: pointer;
    }

    input[type="submit"]:hover {
      background-color: #140a34;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1 class="title">Simple Blog using NodeJs</h1>
    <h5 class="author">by Irivinti Gayathri</h5>
  </div>
  <div class="container">
    <form action="/create" method="POST" enctype="multipart/form-data">
      <input type="text" name="title" placeholder="Post title" required autofocus="on" minlength="4"/>
      <textarea name="content" placeholder="Post Content..." required minlength="30"></textarea>
      <input type="file" name="image" required/>
      <input type="text" name="name" placeholder="Your name" required minlength="4"/>
      <input type="submit" value="Publish" />
    </form>
  </div>
</body>
</html>
